# حالة المشروع الكاملة — دليل التطبيق والتسليم

**تاريخ آخر تحديث:** 22 أغسطس 2026

---

## 1) الوضع الحالي — إيه اللي شغّال دلوقتي بالفعل

### الباك اند الرئيسي (`combined_app.py`) — منشور وحي على الإنترنت

```
🔗 https://student-risk-prediction-api-vhgn.onrender.com
📦 GitHub: https://github.com/7assaAhmed/student-risk-prediction-api
```

| Endpoint | الوظيفة | الحالة |
|---|---|---|
| `GET /health` | حالة السيرفر والموديلات | ✅ مُختبَر على Render |
| `GET /student/<id>` | بيانات طالب (سنة1-3) | ✅ مُختبَر |
| `GET /student/<id>/courses` | تاريخ المقررات + مخاطر مقررات قادمة | ✅ مُختبَر |
| `POST /predict` | كل التنبؤات (إنذار، ترقية، تأخر، GPA، تخرج، تجميع) | ✅ مُختبَر |
| `POST /predict-next-term` | توقع **الترم الجاي** بس (مش السنة كلها) | ✅ **جديد، مُختبَر محلياً — لسه محتاج رفع** |
| `POST /chat` | المستشار الأكاديمي الذكي | ✅ مُختبَر، بعد إصلاح التنسيق والترميز |

---

## 2) الخطوات اللي لازم تعمليها دلوقتي — بالترتيب

### خطوة 1: حدّثي `combined_app.py` على GitHub (فيه الـ`/predict-next-term` الجديد)

```powershell
cd "C:\Users\h7882\Desktop\مشروع التخرج\combined_deploy_package"
# نزّلي واستبدلي combined_app.py الجديد هنا
```

### خطوة 2: ضيفي الموديل الجديد

نزّلي `next_term_gpa_model.joblib` وحطّيه جوه فولدر `ml_models/`.

### خطوة 3: ارفعي على GitHub

```powershell
git add .
git commit -m "Add next-term GPA prediction endpoint"
git push origin main
```

Render هيعيد النشر تلقائياً خلال دقيقتين.

---

## 3) الأنظمة المساعدة — منفصلة عن الباك اند الرئيسي عمداً

| الملف | الغرض | لسه شغّال؟ | بورت |
|---|---|---|---|
| `mock_features_server.py` | بيانات **وهمية** (Progress/Notifications/Attendance/Assignments/Exams) لتجربة واجهة Flutter بس | اختياري، شغّليه لو عايزة تجربي شكل الشاشات دي | 5001 |
| `engagement_tracker.py` | قاعدة بيانات **حقيقية فاضية** — هتتملى فعلياً لما التطبيق يشتغل حقيقي (حضور/كويزات/واجبات/ميدتيرم) | **شغّليه من دلوقتي** عشان يبدأ يجمّع بيانات حقيقية | 5002 |

⚠️ **الفرق الجوهري بينهم:** الأول بيانات مُختلَقة موسومة بوضوح، للعرض بس. التاني فاضي تماماً وهيتملى ببيانات حقيقية فعلية بمرور الوقت.

---

## 4) الفرونت اند — نسخ تجريبية جاهزة (React، للمرجعية والاختبار)

| الملف | المحتوى | الحالة |
|---|---|---|
| `HomeScreen.jsx` | الصفحة الرئيسية + أيقونة شات بوت | ✅ شغّالة، جُرِّبت بصرياً |
| `StudentAnalytics.jsx` | Overview/Warnings/Clustering (3 تابات) | ✅ شغّالة، فيها مخاطر لكل مقرر حقيقية |
| `GradeHistory.jsx` | سجل الدرجات الفصلي | ✅ جديدة، بيانات حقيقية 100% |
| `test_page.html` | نسخة كل حاجة في ملف واحد بسيط (بدون React) | ✅ للاختبار السريع |

**هذه النسخ React للتجربة المحلية بس** — مش هتتنشر، والتطبيق الحقيقي هيبقى Flutter زي ما اتفقنا.

---

## 5) للتسليم لزميلك في Flutter

| الملف | المحتوى |
|---|---|
| `analytics_api_client.dart` | كود Dart كامل، كل الـmodels وطرق الاستدعاء لكل الـendpoints (بما فيهم `/predict-next-term` الجديد لو حدّثتيه) |
| `API_DOCUMENTATION.md` | توثيق كامل بصيغة JSON لكل endpoint |

**الرسالة الجاهزة لزميلك:**
> "شغّل `combined_app.py`، أو استخدم الرابط المنشور مباشرة `https://student-risk-prediction-api-vhgn.onrender.com`. `analytics_api_client.dart` فيه كل الـmodels والدوال جاهزة — غيّر بس الـ`baseUrl` لو هتشتغل محلياً."

---

## 6) القرارات المهمة اللي اتاخدت (ولوجيهها لو اتسألتِ في المناقشة)

| القرار | السبب |
|---|---|
| RandomForest موحّد في أغلب الأنظمة | بساطة الكود + توافق SHAP، رغم إن موديلات تانية كانت أدق شكلياً في حالات معدودة |
| الترقية اتبدّلت لـLogisticRegression | اختبار حقيقي أثبت تفوّقه (recall 86% مقابل 60%)، واتأكد استقراره على حالات فردية |
| توقع GPA رجع RandomForest | LinearRegression أعطى تنبؤات معكوسة (خطر عدم استقرار حقيقي)، رغم دقة متوسطة أعلى شكلياً |
| مفيش بيانات حضور/كويزات مُختلَقة في الموديلات الأساسية | حفاظاً على نزاهة البحث العلمي للمشروع |
| فجوة "سنة2/ربيع" اتعالجت بـMedian Imputation | تقنية إحصائية معيارية وموثّقة، مش اختلاق |

---

## 7) خطوات لاحقة (لما تكوني جاهزة)

1. **اربطي `combined_app.py` بمشروع Flutter الفعلي** باستخدام `analytics_api_client.dart`
2. **شغّلي `engagement_tracker.py`** واستخدميه فعلياً كل ما يتسجّل حضور/درجة حقيقية
3. **بعد فصل دراسي كامل من البيانات الحقيقية**، ارجعي لينا ندرّب موديل "الإنذار قبل الفاينال" الحقيقي على البيانات المتجمّعة
